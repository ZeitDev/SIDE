description: Experiment 07 - Fixed Task Weighting

training:
  tasks:
    segmentation:
      enabled: true
    disparity:
      enabled: true

  weighting:
    inter:
      name: criterions.weighting.Unweighted
      params: {}