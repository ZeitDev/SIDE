description: Experiment 02 - Raw Dataset / No Confidence Gate

data:
  confidence_gate: 0.0 # Disable confidence gate to use all samples for disparity training

training:
  tasks:
    segmentation:
      enabled: true
    disparity:
      enabled: true